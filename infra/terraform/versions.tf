terraform {
  required_version = ">= 1.5"

  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 5.50"
    }
  }

  backend "s3" {
    # Configure via: terraform init -backend-config=...  (see scripts/deploy.sh)
  }
}
